/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    env: {
    
      
    },
    images: {
      domains: ["res.cloudinary.com"],
    },


};

export default nextConfig;
